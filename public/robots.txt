User-agent: *
Allow: /

# Keep this host in sync with `siteUrl` in src/data/site.ts.
# robots.txt is a static file and cannot import that constant.
Sitemap: https://blackswampai.com/sitemap-index.xml
